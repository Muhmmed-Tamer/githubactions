on:
  workflow_dispatch:
jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: Set API key
        run: echo "API_KEY=12345" >> $GITHUB_ENV # Rediractions In Github Environment
      - name: Use API Key
        run:  echo "Using API Key $API_KEY"
