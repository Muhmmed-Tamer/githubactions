on:
  workflow_dispatch: 
jobs:
  expressions:
    runs-on: ubuntu-latest
    
    steps:      
      - name: Print a Message
        run: echo "This Job Is Running ....."
        #if: ${{github.event_name == 'push' || github.ref == 'refs/head/master'}}
        #if: ${{contains()}}
        #if: ${{startsWith()}}
        #if: ${{failure()}}
        #if: ${{success()}} 
        #if: always() # alaways runs regardless of the previous step result
        #if: cancelled()
        
