name: FirstWorkflow
on:
  push: 
    branches:
      - master
jobs:
  example_job:
    runs-on: ubuntu-latest

    steps:
      - name: Print welcome Message
        run: echo "Welcome to our first Github Workflow!"
